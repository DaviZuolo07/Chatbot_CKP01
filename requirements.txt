# LangChain 0.3+ (versão adotada no semestre — Aula 01).
# langchain-core + langchain-ollama: base LCEL/ChatOllama usada no projeto todo.
# langchain: pacote "clássico" (langchain.chains.ConversationChain,
# langchain.memory.ConversationTokenBufferMemory), exigido literalmente pela
# Aula 02. Havia sido removido porque, no Python 3.14, essas classes não
# importavam (dependência pydantic v1-style incompatível). No ambiente atual
# do projeto (Python 3.13.9) o import funciona normalmente — ver chain.py,
# função criar_chain_conversa, e memory_manager.py.
langchain-core>=0.3,<0.4
langchain-ollama>=0.3,<0.4
langchain>=0.3,<0.4

# Cliente Ollama: lê OLLAMA_HOST e OLLAMA_API_KEY direto do ambiente
ollama>=0.6

# Schemas de saída (Aula 03)
pydantic>=2.7,<3

# Carrega o .env para os.environ
python-dotenv>=1.0

# Interface web — Gradio em http://localhost:7860 (enunciado do CKP01)
gradio>=5,<6

# Tabela e gráficos do context rot (já vem com o Gradio; explícito porque importamos)
pandas>=2

# Contagem de tokens na memória e no context rot (diferencial do CKP01)
tiktoken>=0.7