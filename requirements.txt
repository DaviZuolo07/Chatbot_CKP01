# LangChain 0.3+ (versão adotada no semestre — Aula 01).
# Usamos só langchain-core + langchain-ollama: o pacote "langchain" clássico
# (langchain.chains, langchain.memory) foi removido do projeto porque é
# pydantic v1-style por baixo e não constrói no Python 3.14 (ver memory_manager.py
# e chain.py). A memória (Aula 02) é implementada manualmente com LCEL puro —
# o próprio material da Aula 02 cita essa abordagem (RunnableWithMessageHistory
# e LCEL) como "a moderna para memória em 0.3+".
langchain-core>=0.3,<0.4
langchain-ollama>=0.3,<0.4

# Cliente Ollama: lê OLLAMA_HOST e OLLAMA_API_KEY direto do ambiente
ollama>=0.6

# Schemas de saída (Aula 03)
pydantic>=2.7,<3

# Carrega o .env para os.environ
python-dotenv>=1.0

# Interface web — Gradio em http://localhost:7860 (enunciado do CKP01)
gradio>=5,<6

# Tabela e gráficos do context rot (já vem com o Gradio; explícito porque importamos)
pandas>=2

# Contagem de tokens na memória e no context rot (diferencial do CKP01)
tiktoken>=0.7